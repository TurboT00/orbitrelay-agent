story_id: e07s04
title: Disconnect and log out of Codex with explicit ownership
spec: e07s04-codex-disconnect-ownership.md
type: fix
context: domain
risk: P0
status: failing
bcps: 8
tasks:
  - id: 1
    description: Add lifecycle contracts for metadata-only disconnect, selection clearing, delegated logout, and combined logout-plus-disconnect.
    verify: "uv run python -m unittest tests.test_provider_cli tests.test_codex_bridge -v"
    risk: P0
    security: high
    status: failing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Codex Lifecycle", "ownership", "Security Review"]
  - id: 2
    description: Keep plain disconnect from changing official authentication and keep plain logout from removing OrbitRelay metadata.
    verify: "uv run python -m unittest tests.test_provider_cli tests.test_codex_bridge tests.test_connection_service -v"
    risk: P0
    security: high
    status: failing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Codex Lifecycle", "boundaries", "Security Review"]
  - id: 3
    description: Execute the combined operation logout-first and report every unchanged, complete, or partial state truthfully without automatic provider fallback.
    verify: "uv run python -m unittest tests.test_provider_cli tests.test_codex_bridge tests.test_cli_connections -v"
    risk: P0
    security: high
    status: failing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Codex Lifecycle", "partial result", "Security Review"]
