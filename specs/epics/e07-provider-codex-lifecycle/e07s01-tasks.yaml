story_id: e07s01
title: Inspect API-provider readiness offline
spec: e07s01-offline-provider-readiness.md
type: fix
context: domain
risk: P0
status: passing
bcps: 8
tasks:
  - id: 1
    description: Add status contracts for configured, selected, catalog/model, and present, absent, or unavailable credential facts.
    verify: "uv run python -m unittest tests.test_provider_cli tests.test_connection_service -v"
    risk: P0
    security: high
    status: passing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Provider Readiness", "facts", "Security Review"]
  - id: 2
    description: Derive local-ready, not-ready, or unknown without contacting a provider or exposing a credential value.
    verify: "uv run python -m unittest tests.test_provider_cli tests.test_connection_service tests.test_credentials -v"
    risk: P0
    security: high
    status: passing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Provider Readiness", "credentials", "Security Review"]
  - id: 3
    description: Keep provider listing metadata-only and let status survive a missing or unavailable native credential backend.
    verify: "uv run python -m unittest tests.test_provider_cli tests.test_cli_connections -v"
    risk: P0
    security: high
    status: passing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Provider Readiness", "CLI", "Security Review"]
