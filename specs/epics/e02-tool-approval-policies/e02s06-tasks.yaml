story_id: e02s06
title: Audit approval decisions without secret leakage
status: failing
bcps: 3
tasks:
  - id: 1
    description: Add allowlisted approval records and stable reason tests across every policy outcome.
    verify: "uv run python -m unittest tests.test_approvals -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P1
    security: high
    status: passing
    allure:
      severity: high
      categories: [unit, Audit, Security Review]
  - id: 2
    description: Remove raw verbose argument printing and add bounded control-safe decision formatting.
    verify: "uv run python -m unittest tests.test_tools tests.test_redaction -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P1
    security: high
    status: passing
    allure:
      severity: high
      categories: [integration, Redaction, Security Review]
  - id: 3
    description: Emit ordered verbose events to stderr without changing nonverbose stdout or tool results.
    verify: "uv run python -m unittest tests.test_cli tests.test_agent -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P1
    security: high
    status: passing
    allure:
      severity: high
      categories: [integration, CLI, Security Review]
  - id: 4
    description: Add adversarial secret, control-character, oversized-value, and correlation fixtures.
    verify: "uv run python -m unittest tests.test_approvals tests.test_redaction tests.test_agent -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P1
    security: high
    status: passing
    allure:
      severity: high
      categories: [regression, Audit, Security Review]
  - id: 5
    description: Run the complete offline regression, package build, and isolated-wheel gate.
    verify: "./scripts/check.sh && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P1
    security: high
    status: failing
    allure:
      severity: high
      categories: [regression, Packaging, Security Review]
