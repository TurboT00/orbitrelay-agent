story_id: e02s02
title: Approve or deny local Python execution
status: failing
bcps: 5
tasks:
  - id: 1
    description: Add execution approval preview and decision tests with hostile argument fixtures.
    verify: "uv run python -m unittest tests.test_approvals -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: passing
    allure:
      severity: critical
      categories: [unit, Approval, Security Review]
  - id: 2
    description: Prepare and validate Python execution without launching a process.
    verify: "uv run python -m unittest tests.test_tools tests.test_sandbox -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: failing
    allure:
      severity: critical
      categories: [integration, Process Execution, Security Review]
  - id: 3
    description: Gate subprocess creation through complete-batch authorization and preserve correlated outcomes.
    verify: "uv run python -m unittest tests.test_agent -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: failing
    allure:
      severity: critical
      categories: [integration, Agent Loop, Security Review]
  - id: 4
    description: Exercise CLI approve/deny execution using fake input and a fake subprocess boundary.
    verify: "uv run python -m unittest tests.test_cli tests.test_approvals -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: failing
    allure:
      severity: critical
      categories: [integration, CLI, Security Review]
  - id: 5
    description: Run execution security regressions and prove no process starts before approval.
    verify: "uv run python -m unittest tests.test_agent tests.test_tools tests.test_sandbox tests.test_approvals -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: failing
    allure:
      severity: critical
      categories: [regression, Sandbox, Security Review]
