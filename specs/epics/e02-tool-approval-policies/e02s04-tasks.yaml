story_id: e02s04
title: Run safely in read-only or unattended contexts
status: failing
bcps: 5
tasks:
  - id: 1
    description: Add read-only and unavailable-input policy tests with fake timeout and EOF sources.
    verify: "uv run python -m unittest tests.test_approvals -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: failing
    allure:
      severity: critical
      categories: [unit, Fail Closed, Security Review]
  - id: 2
    description: Add CLI policy and bounded timeout parsing with pre-network validation.
    verify: "uv run python -m unittest tests.test_cli -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: failing
    allure:
      severity: critical
      categories: [integration, CLI, Security Review]
  - id: 3
    description: Apply read-only and fail-closed outcomes across agent batches without prompting or side effects.
    verify: "uv run python -m unittest tests.test_agent tests.test_tools -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: failing
    allure:
      severity: critical
      categories: [integration, Agent Loop, Security Review]
  - id: 4
    description: Preserve confined reads and sandbox failures under every unattended path.
    verify: "uv run python -m unittest tests.test_tools tests.test_sandbox -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: failing
    allure:
      severity: critical
      categories: [regression, Sandbox, Security Review]
  - id: 5
    description: Run EOF, timeout, malformed-input, and noninteractive security regressions.
    verify: "uv run python -m unittest tests.test_cli tests.test_agent tests.test_tools tests.test_approvals -v && printf '%s\\n' 'no new security findings in affected paths'"
    risk: P0
    security: high
    status: failing
    allure:
      severity: critical
      categories: [regression, Fail Closed, Security Review]
