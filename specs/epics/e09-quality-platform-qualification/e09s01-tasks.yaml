story_id: e09s01
title: Run lint and typing as terminal release gates
spec: e09s01-lint-type-gates.md
type: fix
context: infra
risk: P0
status: passing
bcps: 5
tasks:
  - id: 1
    description: Add reproducibly locked Ruff and mypy tools with project-wide configurations that avoid broad suppressions.
    verify: "uv run ruff check . && uv run mypy src/orbitrelay"
    risk: P1
    security: medium
    status: passing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: high
      categories: ["Quality Gate", "lint typing", "Security Review"]
  - id: 2
    description: Resolve current lint and type findings while preserving public runtime contracts and test doubles.
    verify: "uv run ruff check . && uv run mypy src/orbitrelay && uv run python -m unittest discover -s tests -v"
    risk: P0
    security: medium
    status: passing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Quality Gate", "regression", "Security Review"]
  - id: 3
    description: Make the official local check fail whenever lint or typing regresses.
    verify: "./scripts/check.sh"
    risk: P0
    security: medium
    status: passing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Quality Gate", "release check", "Security Review"]
