story_id: e06s05
title: Receive expected CLI failures without traceback leakage
spec: e06s05-cli-failure-streams.md
type: fix
context: infra
risk: P0
status: passing
bcps: 5
tasks:
  - id: 1
    description: Add process-level contracts for missing configuration, unavailable credentials, provider errors, denied reads, and invalid session input.
    verify: "uv run python -m unittest tests.test_cli_errors -v"
    risk: P0
    security: high
    status: passing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["CLI Trust", "process", "Security Review"]
  - id: 2
    description: Translate expected failures into concise nonzero CLI results without traceback or secret-bearing exception detail.
    verify: "uv run python -m unittest tests.test_cli_errors tests.test_redaction -v"
    risk: P0
    security: high
    status: passing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["CLI Trust", "redaction", "Security Review"]
  - id: 3
    description: Preserve final-answer-only stdout and diagnostic stderr in streamed, non-streamed, and tool-call failure paths.
    verify: "uv run python -m unittest tests.test_cli_errors tests.test_streaming tests.test_agent -v"
    risk: P0
    security: high
    status: passing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["CLI Trust", "streams", "Security Review"]
