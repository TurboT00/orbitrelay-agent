story_id: e08s01
title: Prevent concurrent ownership of one active session
spec: e08s01-exclusive-session-owner.md
type: fix
context: domain
risk: P0
status: failing
bcps: 8
tasks:
  - id: 1
    description: Add real multi-process contracts for immediate conflict, bounded wait, timeout, crash release, and independent sessions.
    verify: "uv run python -m unittest tests.test_session_concurrency -v"
    risk: P0
    security: high
    status: failing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Session Concurrency", "process", "Security Review"]
  - id: 2
    description: Hold exclusive ownership from history load through provider and tool work to the final durable checkpoint.
    verify: "uv run python -m unittest tests.test_session_concurrency tests.test_sessions tests.test_agent -v"
    risk: P0
    security: high
    status: failing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Session Concurrency", "ownership", "Security Review"]
  - id: 3
    description: Fail before any provider request on conflict while allowing secret-free list and show commands to identify active sessions.
    verify: "uv run python -m unittest tests.test_session_concurrency tests.test_sessions tests.test_cli_errors -v"
    risk: P0
    security: high
    status: failing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Session Concurrency", "CLI", "Security Review"]
