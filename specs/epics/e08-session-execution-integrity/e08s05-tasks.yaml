story_id: e08s05
title: Bound local tool and Codex process output
spec: e08s05-bounded-process-output.md
type: fix
context: infra
risk: P0
status: failing
bcps: 5
tasks:
  - id: 1
    description: Add large-output and timeout contracts for Python execution and official Codex subprocess paths.
    verify: "uv run python -m unittest tests.test_tools tests.test_codex_bridge -v"
    risk: P0
    security: high
    status: failing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Bounded Output", "process", "Security Review"]
  - id: 2
    description: Enforce deterministic output limits without deadlock and report truncation metadata instead of unbounded content.
    verify: "uv run python -m unittest tests.test_tools tests.test_codex_bridge tests.test_events -v"
    risk: P0
    security: high
    status: failing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Bounded Output", "events", "Security Review"]
  - id: 3
    description: Keep bounded failures and summaries free of raw arguments, credentials, and discarded output content.
    verify: "uv run python -m unittest tests.test_tools tests.test_codex_bridge tests.test_run_summary tests.test_redaction -v"
    risk: P0
    security: high
    status: failing
    security_acceptance: "no new security findings in affected paths"
    allure:
      severity: critical
      categories: ["Bounded Output", "redaction", "Security Review"]
